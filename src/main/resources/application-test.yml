spring:
  profiles:
    active: test

  datasource:
    url: jdbc:tc:postgresql:14-alpine:///shop-test-db
  sql:
    init:
      mode: always

  jpa:
    open-in-view: true
    show-sql: true
    properties:
      hibernate:
        format_sql: true

